services:
  qb-up-limit:
    image: ghcr.io/dlovew/qb-up-limit:main
    container_name: qb-up-limit
    restart: unless-stopped
    network_mode: host
    environment:
      TZ: Asia/Shanghai
    volumes:
      - ./data:/data

